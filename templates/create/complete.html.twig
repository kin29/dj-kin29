{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block body %}
    {{ parent() }}

    <div class="example-wrapper">
        プレイリスト作成が完了しました🍺
        {% for track in tracks %}
            - {{ track }}
        {% endfor %}
        {% for artist in artists %}
            - {{ artist }}
        {% endfor %}
    </div>
{% endblock %}
